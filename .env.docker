# Environment variables for Docker Compose
# Use this when running with `docker compose up` (services run in the same compose network).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=montsystem
DATABASE_HOST=db
# Production/CI should override JWT_SECRET with a secure value
JWT_SECRET=dev-secret-montsystem
